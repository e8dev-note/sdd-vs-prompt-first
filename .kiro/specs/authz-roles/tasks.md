# Implementation Plan

- [ ] 1. 基盤: ロールと権限表
- [x] 1.1 ロール列のマイグレーションを追加する
  - 連番 004 として users にロール列(必須、既定 viewer、3 値の CHECK)を追加し、初期ユーザー admin / editor / viewer に username と同じロールを付ける UPDATE を続ける
  - 既存の migration テストの期待値を 4 件に更新し、003 まで適用済みでユーザーのある DB を開くと 004 が適用されロールが username になり他の列が不変であることをテストする
  - 完了状態: 上記テストが通る
  - _Requirements: 1.1, 1.2, 1.3, 1.6, 6.1_

- [x] 1.2 (P) 権限表と判定の純粋モジュールを実装する
  - ロール 3 値の型と解釈(3 値以外は viewer)、操作 4 種の型、ロールごとの許可集合(viewer: 閲覧。editor: + 編集 + ブックマーク切り替え。admin: + 削除)、判定関数
  - テスト: 3 ロール × 4 操作の 12 通りを表で明示、解釈の正常値と不正値(大文字、null、空、未知)
  - 完了状態: 上記テストが通り、モジュールは DB・Next に依存しない
  - _Requirements: 1.5, 2.1, 2.2, 2.3, 2.4, 2.5, 6.1_
  - _Boundary: Authz_

- [x] 1.3 ユーザーにロールを載せる
  - ユーザー型にロールを追加し、取得・認証の結果に含める(読み取り時に解釈関数で丸める)。初期ユーザーの定義と投入にロールを含める
  - 既存のユーザーテストの期待キーを更新し、シード直後と認証結果のロールが username と一致することをテストする
  - 完了状態: 上記テストが通り、既存テストも通る
  - _Depends: 1.1, 1.2_
  - _Requirements: 1.4, 6.1_

- [ ] 2. コア: サーバ側の検査と拒否の着地先
- [x] 2.1 権限検査関数と権限なし画面を追加する
  - ログイン検査の後に判定を行い、不許可なら権限なし画面へ遷移させる関数を認証モジュールに追加する。要求に含まれる値は使わない
  - 権限なし画面(見出し、説明、一覧へのリンク)を追加する。ログインなしでも表示できる
  - 完了状態: 関数を呼ぶ action が、権限のないロールでは画面へ遷移し、未ログインでは従来どおり `/login` へ
  - _Requirements: 4.4, 4.6, 4.7, 4.8_

- [x] 2.2 3 つの Server Action に権限検査を挿入する
  - 削除は admin のみ、ブックマーク切り替えは editor 以上を、遷移型の検査で。編集の保存は editor 以上を判定し、不許可ならモーダル内メッセージを状態で返す
  - いずれもデータ変更より前に置く
  - 完了状態: 権限のないロールの要求でデータが変わらず、遷移型は権限なし画面、更新はメッセージ
  - _Depends: 2.1_
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 4.6_

- [ ] 3. 統合と完了条件
- [ ] 3.1 画面の出し分けとヘッダのロール表示を実装する
  - 詳細: 編集ボタンは editor 以上、削除ボタンは admin のみ表示。ブックマーク切り替えは editor 以上に表示し、viewer には印だけ
  - 一覧: 各行のブックマーク切り替えを editor 以上に表示し、viewer には印だけ。行の強調は従来どおり
  - ブックマーク部品に「切り替え可否」の指定を追加し、不可なら印のみを描く
  - ヘッダに username に加えてロールを表示する
  - 完了状態: viewer / editor / admin それぞれでボタンの有無とヘッダ表示が権限表どおり
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 5.1_

- [ ] 3.2 完了条件を検証し README にロール表を書く
  - README にロールと操作の対応表を追加する
  - `npm run build`、`npm run lint`、`npm test` を全て通す
  - curl(偽装要求): viewer のセッションで削除・トグル POST → 権限なし画面へ遷移し DB 不変。viewer で更新 POST → メッセージ、DB 不変。editor で削除 → 権限なし画面、トグルと更新は成功。admin で削除成功。未ログインは `/login`
  - curl / ブラウザ(表示): 3 ロールの詳細 HTML でボタンの有無、一覧の行にフォームがあるか、ヘッダにロール
  - 完了状態: 3 コマンドが成功し、上記の確認が全て権限表どおり
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 4.1, 4.2, 4.3, 4.4, 4.5, 4.8, 5.1, 5.2, 6.2, 6.3_
