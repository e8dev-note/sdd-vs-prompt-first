# Implementation Plan

- [ ] 1. 基盤: スキーマとデータ層
- [x] 1.1 ブックマーク列のマイグレーションを追加する
  - 連番 002 として、products にブックマークフラグ列(整数、必須、既定 0)を追加する 1 文を書く。既存ファイルは編集しない
  - 既存の migration テストの期待値を 2 件に更新し、001 だけ適用済みでデータのある DB を開くと 002 が適用され既存行が OFF になることをテストする
  - 完了状態: 上記テストが通る
  - _Requirements: 2.1, 2.2, 5.1_

- [x] 1.2 リポジトリにブックマークの読み取り・更新・フィルタを追加する
  - 商品型にブックマーク(boolean)を追加し、DB 行からの変換を 1 か所に集約して一覧・1 件取得の両方で使う
  - 一覧取得に「ブックマークのみ」条件を追加し、検索条件・並び順と AND で組み合わせる
  - ブックマークを設定する関数を追加する(影響行数を返す。updated_at は変更しない)
  - テスト: 既定は全件 OFF、ON/OFF の往復と影響行数、不在で 0、updated_at 不変、フィルタ単独・検索併用・並び替え併用、削除でフラグごと消える
  - 完了状態: 上記テストが通り、既存テストも通る
  - _Requirements: 1.3, 1.4, 2.1, 2.3, 2.4, 3.6, 5.1_

- [x] 1.3 一覧 URL 規則にフィルタと戻り先の検証を追加する
  - URL 組み立てにブックマークフィルタを加える(有効時だけ `bookmarked=1`、順序は q, sort, order, bookmarked)
  - クエリ値の解釈(`1` のときだけ有効)と、戻り先 URL の検証(`/products` で始まる内部パスのみ、それ以外は一覧)を純粋関数で用意する
  - テスト: 解釈の 4 パターン、3 条件の組み合わせと順序、戻り先の許可・拒否(外部 URL、`//`、別パス、null)
  - 完了状態: 上記テストが通る
  - _Requirements: 1.5, 3.2, 3.3, 3.5, 3.7, 3.8, 5.1_

- [ ] 2. コア: トグルとフィルタ
- [x] 2.1 ブックマークトグルの Server Action と部品を実装する
  - フォームから id、次の状態、戻り先を受け取り、設定 → 一覧と詳細の再検証 → 検証済みの戻り先へ遷移する。不正・不在なら一覧へ
  - トグル部品はフォーム(JS 不要)で、hidden に id・次の状態・戻り先を持ち、ボタンは ON で ★、OFF で ☆。状態を `aria-pressed` で伝える。詳細用に文字ラベル付きの表示も持つ
  - 完了状態: 部品を置いた画面でボタンを押すと状態が反転し、元の URL に戻る
  - _Requirements: 1.3, 1.4, 1.5, 1.6, 1.7, 1.8, 3.9, 4.3_

- [x] 2.2 (P) フィルタ切り替え部品を実装し、ヘッダと検索フォームにフィルタを通す
  - 「ブックマークのみ表示」のリンクを作る。押すとフィルタを反転した一覧 URL へ遷移し、検索条件と並び順を保持する。現在の状態を `aria-pressed` と見た目で示す
  - ソートヘッダの URL にフィルタを含める。検索フォームはフィルタ有効時に hidden で引き継ぎ、クリアのリンクも保持する
  - 完了状態: フィルタ有効の一覧で並び替え・検索・クリアをしてもフィルタが残り、フィルタ切り替えで検索条件と並び順が残る
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.7, 3.8, 4.3_
  - _Boundary: BookmarkFilterToggle, SortHeader, SearchForm_

- [ ] 3. 統合と完了条件
- [ ] 3.1 一覧画面と詳細画面に配線する
  - 一覧: URL のフィルタを解釈して一覧取得・ヘッダ・検索フォーム・フィルタ切り替えに渡す。各行の先頭にトグル(戻り先は現在の一覧 URL)を置き、ON の行を強調する。フィルタ有効で 0 件なら「ブックマークした商品はありません」
  - 詳細: 定義リストにブックマーク状態の行を追加し、編集・削除の横にラベル付きトグル(戻り先は同じ詳細)を置く
  - 完了状態: 一覧で ☆ を押すと ★ になり行が強調され URL は同じ。詳細で「ブックマーク中」の表示とトグルが動く
  - _Depends: 2.1, 2.2_
  - _Requirements: 1.1, 1.2, 1.5, 1.6, 3.4, 3.6, 3.10, 4.1, 4.2_

- [ ] 3.2 完了条件を検証する
  - `npm run build`、`npm run lint`、`npm test` を全て通す
  - ブラウザで: 一覧で ☆ → ★(行が強調、URL 同じ)→ フィルタ ON(`?bookmarked=1`、その商品だけ)→ 検索してもフィルタ維持 → 並び替えてもフィルタと検索語維持 → フィルタ中に ★ を外すと行が消える → 詳細で「ブックマーク中」とトグル → `?bookmarked=x` は無効扱い。curl でフォーム POST(JS なし)でも切り替わる
  - 完了状態: 3 コマンドが成功し、上記の流れが動く
  - _Requirements: 5.1, 5.2_
