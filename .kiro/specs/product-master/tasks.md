# Implementation Plan

- [ ] 1. 基盤: 設定、スキーマ、DB 初期化、テスト基盤
- [x] 1.1 プロジェクト設定とテスト基盤を整える
  - ネイティブ DB ドライバをサーバ側の外部パッケージとして扱う設定を入れる
  - ルート URL から商品一覧へ誘導するリダイレクト設定を入れる
  - Vitest を node 環境・`@/` エイリアス付きで動かす設定と `npm test` スクリプトを追加する
  - DB ファイル置き場を用意し、DB ファイル本体を git 管理外にする
  - 完了状態: `npm test`(テスト 0 件)、`npm run lint`、`npm run build` が通る
  - _Requirements: 1.6, 6.2_

- [x] 1.2 商品テーブルのマイグレーションを作成する
  - 設計の物理データモデルどおりに products テーブルを定義する(code 一意、price は 0 以上の整数、時刻列は必須)
  - 連番 001 のファイルとして追加し、以後編集しない
  - 完了状態: SQLite にそのまま流して products テーブルが作成される
  - _Requirements: 5.4_

- [ ] 1.3 DB 接続とマイグレーション適用の仕組みを実装する
  - プロセス内で 1 つの接続を保持し、HMR で多重に開かない
  - DB パスは環境変数で差し替え可能、既定は `data/app.db`。親ディレクトリがなければ作る
  - 適用済み migration を記録するテーブルを持ち、未適用分をファイル名昇順に 1 ファイル 1 トランザクションで適用する
  - テスト用に接続を閉じて再初期化する手段を用意する
  - テスト: 初回で migration が適用され、2 回目の接続取得で重複適用されない。一時ディレクトリの DB を使い `data/app.db` を汚さない
  - 完了状態: 上記テストが通る
  - _Requirements: 5.1, 6.1, 6.3_

- [ ] 1.4 初期データの定義と投入を実装する
  - 20 件程度の商品(code 一意、category は数種類、note は一部のみ)を定義する
  - products が 0 件のときだけ投入し、created_at と updated_at に投入時刻(ISO8601 UTC)を入れる
  - DB 初期化の最後に投入処理を呼ぶ
  - テスト: 空 DB で 20 件投入される。再度呼んでも件数が増えない。既存データがある DB では投入されない
  - 完了状態: 上記テストが通る
  - _Requirements: 5.2, 5.3, 6.3_

- [ ] 2. コア: 商品データアクセス
- [ ] 2.1 商品リポジトリを実装する
  - 一覧取得: 常に code 昇順(同値は id 昇順)。検索語が指定されたら code / name / category の部分一致で絞り込む
  - 検索語は前後の空白を除き、空なら全件。`%`、`_`、`\` はエスケープして通常文字として一致させる。英字の大文字小文字は区別しない
  - 1 件取得: 存在しなければ null
  - 削除: 削除行数を返し、存在しなくても例外にしない
  - URL の id 文字列を検証して数値にする関数(非数値・負数・小数・空は null)
  - ISO8601 UTC の現在時刻を返す関数
  - テスト: 順序、部分一致、大小無視、`%` と `_` の文字どおり一致、空白のみで全件、不在 id、id 解析の境界、削除の冪等性
  - 完了状態: 上記テストが通り、`npm test` の合計が 1.3〜2.1 の分だけ増えている
  - _Requirements: 1.1, 1.4, 2.2, 2.5, 2.6, 2.7, 3.1, 3.3, 3.4, 4.3, 4.5, 4.6, 6.3_

- [ ] 3. コア: 画面と操作
- [ ] 3.1 (P) 商品一覧画面と検索フォームを実装する
  - URL クエリ `q` を読み取り、リポジトリで絞り込んだ一覧を表形式で表示する(列: code, name, category, price)
  - 各行の code から詳細画面へ遷移できる
  - 画面上部に GET フォームの検索ボックスを置き、送信で `?q=` が URL に付く。現在の検索語をボックスに表示する
  - 0 件のときは該当なしの旨と検索語を表示する
  - 完了状態: `/products?q=xxx` にアクセスすると絞り込まれた表が出て、リロードしても検索語が保持される
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 2.4, 2.5, 2.8_
  - _Boundary: ProductsPage, SearchForm_

- [ ] 3.2 (P) 商品詳細画面と 404 画面を実装する
  - URL の id を検証し、非数値または不在なら 404 画面を返す
  - 全 8 列を表示し、note が未設定なら空欄にする
  - 一覧へ戻る導線を置く
  - 404 画面には見つからない旨と一覧へのリンクを置く
  - 完了状態: 存在する id で全列が表示され、`/products/999999` と `/products/abc` が HTTP 404 になる
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_
  - _Boundary: ProductDetailPage_

- [ ] 3.3 削除操作を実装して詳細画面に組み込む
  - フォームから id を受け取り、リポジトリで削除し、一覧を再検証してから一覧へ遷移する Server Action を作る。不正・不在 id でも例外にせず一覧へ遷移する
  - 削除ボタンはクライアント側で確認ダイアログを出し、取り消しなら送信しない
  - 詳細画面に削除ボタンを置く
  - 完了状態: 詳細画面の削除で確認が出て、承認すると一覧に戻りその商品が消えている。取り消しでは詳細画面に留まる
  - _Depends: 3.2_
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 4.6_

- [ ] 4. 統合と完了条件
- [ ] 4.1 共通レイアウトとスタイルを整える
  - scaffold のトップページとダークモード用 CSS を取り除き、ライトテーマで表と文字が読める配色にする
  - 共通レイアウトにアプリ名のヘッダと一覧へのリンクを置く
  - 完了状態: `/` にアクセスすると `/products` に誘導され、一覧と詳細が同じレイアウトで表示される
  - _Requirements: 1.6, 6.1_

- [ ] 4.2 完了条件を検証し起動手順を記載する
  - `npm run build`、`npm run lint`、`npm test` を全て通す。build で `data/app.db` が生成されないことを確認する
  - `npm run dev` でブラウザから一覧 → 検索 → リロード → 詳細 → 削除(取り消し / 承認)→ 一覧の流れを確認する
  - README を起動手順(Node バージョン、`npm install`、`npm run dev`、アクセス先、テスト・ビルドの実行方法)に置き換える
  - 完了状態: 3 コマンドが全て成功し、README の手順どおりに起動できる
  - _Requirements: 6.1, 6.2, 6.3, 6.4_
