# Implementation Plan

- [ ] 1. 基盤: ソート指定の解釈規則と URL 組み立て
- [x] 1.1 ソート列・方向の型とホワイトリストをリポジトリに定義する
  - 許容する列(code, name, category, price)と方向(asc, desc)を型と定数として公開する
  - 一覧取得のオプションに列と方向を追加する(省略時は従来どおり code 昇順)
  - 完了状態: 既存テストが変更なしで通り、型が他モジュールから参照できる
  - _Requirements: 4.1, 4.3_

- [ ] 1.2 ソート指定の解釈と一覧 URL 組み立ての純粋関数を実装する
  - クエリの生値から有効なソート状態を得る。列が不正・未指定なら既定(code 昇順)、方向が不正・未指定ならその列の昇順
  - ヘッダをクリックしたときの次の状態を決める(同じ列なら反転、別の列なら昇順)
  - 検索語とソート状態から一覧 URL を組み立てる(空の検索語は付けない、ソート未指定なら付けない、値は URL エンコード)
  - テスト: 有効/不正/省略の組み合わせ、反転と切替、`/products` になる条件、日本語と `&` を含む検索語のエンコード
  - 完了状態: 上記テストが通る
  - _Requirements: 1.2, 1.3, 3.5, 3.6, 3.7, 4.1_

- [ ] 2. コア: 並び替えの適用
- [ ] 2.1 一覧取得の ORDER BY を列・方向で可変にする
  - 検索の有無で分かれている SQL の並び順部分を共通化し、指定された列と方向、続けて id 昇順で並べる
  - 列名はホワイトリストに含まれる値のみを SQL に埋め込む(外れた値は既定に丸める)
  - テスト: 4 列それぞれの昇順・降順、price が数値順(0 が先頭、3480 が末尾)、同値(price 120 の 2 件)が id 順、省略時が従来と同一
  - 完了状態: 上記テストが通り、既存テストも通る
  - _Requirements: 1.4, 1.5, 4.1, 5.1_

- [ ] 3. コア: 画面
- [ ] 3.1 (P) ソートヘッダ部品を実装する
  - 列ヘッダをリンクにし、クリックで次のソート状態の URL へ遷移する。検索語を URL に引き継ぐ
  - ソート中の列にだけ方向の印(▲ / ▼)と並び順属性を付け、他の列には付けない
  - 右寄せ(price)にも対応する
  - 完了状態: 現在の状態と列を渡すと、正しい href・印・属性を持つ th が描画される
  - _Requirements: 1.1, 1.2, 1.3, 1.6, 2.1, 2.2, 2.3, 3.3, 4.2_
  - _Boundary: SortHeader_

- [ ] 3.2 (P) 検索フォームがソート状態を引き継ぐようにする
  - ソート状態が渡されたときだけ hidden の sort / order を出力する
  - クリアのリンク先は検索語だけを消し、ソート状態を保持する
  - 完了状態: ソート指定付きの一覧で検索を実行すると、`?q=` と `?sort=`、`?order=` が同時に付いた URL になる
  - _Requirements: 3.4_
  - _Boundary: SearchForm_

- [ ] 4. 統合と完了条件
- [ ] 4.1 一覧ページに配線する
  - URL の sort / order を解釈してソート状態を得て、一覧取得・ソートヘッダ・検索フォームに渡す。URL に sort があるときだけ検索フォームに引き継がせる
  - 4 つの列ヘッダをソートヘッダ部品に置き換える
  - 完了状態: `?sort=price&order=desc` で価格の高い順に並び、price ヘッダに ▼ が付く。指定なしでは従来どおり code 昇順で code に ▲ が付く
  - _Depends: 2.1, 3.1, 3.2_
  - _Requirements: 3.1, 3.2, 4.1, 4.2, 4.3_

- [ ] 4.2 完了条件を検証する
  - `npm run build`、`npm run lint`、`npm test` を全て通す
  - ブラウザで: ヘッダクリックで URL が変わる → 同じヘッダで反転 → リロードで再現 → 検索語を入れて検索してもソートが残る → 別の列に切り替えても検索語が残る → `?sort=x&order=y` で既定表示になりエラーにならない
  - 完了状態: 3 コマンドが成功し、上記の流れが動く
  - _Requirements: 5.1, 5.2_
